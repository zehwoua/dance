<%= form_for [@video, @comment], :class =>"test" do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>
      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_area :comment_text,:class=>"form-control", rows: 4, cols: 30 %>
  </div>
  <div class="actions">
    <%= f.submit "Post Comment", :class => "btn btn-primary pull-right",:style =>"margin-top:1em;"%>
  </div>
<% end %>
